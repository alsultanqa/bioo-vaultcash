import {Request,Response,NextFunction} from 'express'; export function errorHandler(err:any,_req:Request,res:Response,_next:NextFunction){const s=Number(err?.status||err?.statusCode||500); const m=err?.message||'internal_error'; if(s>=500) console.error(err); res.status(s).json({error:m}); }
