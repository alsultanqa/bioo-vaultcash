import crypto from 'crypto'; export function verifyHmac(secret:string, raw:Buffer, sig:string|undefined){ if(!sig) return false; const mac=crypto.createHmac('sha256',secret).update(raw).digest('hex'); return Buffer.from(mac).equals(Buffer.from(sig)); }
