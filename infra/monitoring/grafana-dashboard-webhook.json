{
  "title": "QatarCash \u2014 Webhooks",
  "panels": [
    {
      "type": "graph",
      "title": "Deliveries by status",
      "targets": [
        {
          "expr": "sum by (status) (rate(qc_webhook_deliveries_total[5m]))"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Processing latency p95 (s)",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le) (rate(qc_webhook_processing_seconds_bucket[5m])))"
        }
      ]
    }
  ],
  "schemaVersion": 36
}
